module.exports = {
  root: true,
  // Reminder: preserve Stitch color tokens and avoid hardcoded page-level colors.
  ignorePatterns: [
    'node_modules/',
    'dist/',
    'build/',
    'coverage/',
    '*.min.js'
  ]
}
